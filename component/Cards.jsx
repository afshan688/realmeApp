import React from 'react';
import styles from "./Cards.module.css";

function Cards({imgSrc, title, desc})=> {
  return (
    <div className= {styles.divContainer}>
    <img className={} 
    </div>
  )
}

export default Cards